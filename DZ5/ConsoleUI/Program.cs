using System.Net;
using System.Net.Http.Headers;
using System.Net.Http.Json;
using System.Text.Json;
using System.Text.Json.Serialization;
using TVShowLogic;

namespace ConsoleUI
{
    public class Program
    {
        public static void Main(string[] args)
        {
            //Console.WriteLine("Enter the name of the show you want to search up");
            //string query = Console.ReadLine();

            //var shows = Utilities.SearchShows(query);

            //foreach (Show show in shows)
            //{
            //    Console.WriteLine(show.ToString());
            //}

            //var seasons = Utilities.GetSeasons(shows[0]);

            //foreach(Season season in seasons)
            //{
            //    Console.WriteLine(season.ToString());
            //}

            //var episodes = Utilities.GetEpisodes(seasons[0]);

            //foreach(Episode episode in episodes)
            //{
            //    Console.WriteLine(episode.ToString());
            //}

            //Console.WriteLine();
        }
    }

}